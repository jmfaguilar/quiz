<table>
	<% var i; for (i=0; i < quizes.length; i++) { %>
  		<tr>
        <td><a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
        <td><%= quizes[i].tema %></a></td>
        <% if (session.user) { %>
          <td><a href="/quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
          <td>
            <form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
              <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
                Borrar
              </button>
            </form>
          </td>
        <% } %>
      </tr>
  <% } %>
  <% if (session.user) { %>
    <tr><td><br><a href="/quizes/new"><button type="button">Crear pregunta</button></a></td></tr>
  <% } %>
	<tr><td><br><br>
  		<form method="get" action="/quizes/search">
  		  <b>Búsqueda:</b></td></tr>
  			<tr><td><input type="text" name="search" value="">
  			<input type="submit" value="Buscar"></td></tr>
		  </form>
	</td></tr>
</table>